"""Tools for optimising descriptor mixtures.

This package contains utilities for combining multiple image descriptors
(e.g. Fisher vectors, colour descriptors, global embeddings and shape
features) and searching for an optimal weighting of these components on a
per‑dataset basis."""
